// Top-level build.gradle.kts (Project level)
    plugins {
      id("com.android.application") version "8.3.0" apply false // Specify version and apply false
      id("org.jetbrains.kotlin.android") version "1.9.22" apply false // Specify version and apply false
    }

    // Add repositories here
    repositories {
      google()
      mavenCentral()
    }
